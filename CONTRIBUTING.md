# Contributing

Thanks for your interest!

## Development
1. Create a virtual env
2. `pip install -r requirements.txt`
3. `streamlit run app.py`

## Pull Requests
- Keep the UI text/controls unchanged unless the change is explicitly about UI.
- Prefer small, reviewable PRs.
- Add notes to `TECHNICAL_NOTES.txt` when you change compression logic.
